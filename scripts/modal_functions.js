// Toggles add moda window visibility
function openModal(modal) {
  modal.classList.add("modal_visible")
}

// Toggles add modal window visibility
function closeModal(modal) {
  modal.classList.remove("modal_visible")
}
